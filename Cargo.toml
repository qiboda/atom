# [package]
# name = "terrain"
# version = "0.1.0"
# edition = "2021"

[workspace]
members = ["crates/*", "entries/*"]
resolver = "2"


[workspace.dependencies]
# bevy = { version = "0.12.0", features = ["trace", "trace_tracy", "embedded_watcher"] }
bevy = { version = "0.12.1", features = ["file_watcher"] }
#bevy = { version = "0.12.1", features = [] }
once_cell = "1.19.0"
bevy_common_assets = {version = "0.9.0", features = ["toml"] }

# Enable a small amount of optimization in debug mode
[profile.dev]
opt-level = 1

# Enable high optimizations for dependencies (incl. Bevy), but not for our code:
[profile.dev.package."*"]
opt-level = 3

[profile.release-with-debug]
inherits = "release"
debug = true

# can not patch cross version, otherwie plugin will not work.
# [patch.crates-io]
# bevy = { path = '../bevy' }
# bevy_math = { path = '../bevy/crates/bevy_math' }
