/*!
<auto-generated>
    This code was generated by a tool.
    Changes to this file may cause incorrect behavior and will be lost if
    the code is regenerated.
</auto-generated>
*/

use super::*;

bitflags::bitflags! {
    #[derive(Debug, Hash, Eq, PartialEq)]
    pub struct AccessFlag : u32 {
        const WRITE = 1;
        const READ = 2;
        const TRUNCATE = 4;
        const NEW = 8;
        ///位标记使用示例
        const READ_WRITE = 3;
    }
}
#[derive(bevy::reflect::Reflect, Debug)]
pub struct MultiIndexList {
    pub id1: i32,
    pub id2: i32,
    pub id3: String,
    pub num: i32,
    pub desc: String,
}

impl MultiIndexList {
    pub fn new(mut buf: &mut atom_luban_lib::ByteBuf) -> Result<MultiIndexList, LubanError> {
        let id1 = buf.read_int();
        let id2 = buf.read_int();
        let id3 = buf.read_string();
        let num = buf.read_int();
        let desc = buf.read_string();

        Ok(MultiIndexList {
            id1,
            id2,
            id3,
            num,
            desc,
        })
    }

    pub const __ID__: i32 = 2016237651;
}

#[derive(bevy::reflect::Reflect, Debug)]
pub struct MultiUnionIndexList {
    pub id1: i32,
    pub id2: i32,
    pub id3: String,
    pub num: i32,
    pub desc: String,
}

impl MultiUnionIndexList {
    pub fn new(mut buf: &mut atom_luban_lib::ByteBuf) -> Result<MultiUnionIndexList, LubanError> {
        let id1 = buf.read_int();
        let id2 = buf.read_int();
        let id3 = buf.read_string();
        let num = buf.read_int();
        let desc = buf.read_string();

        Ok(MultiUnionIndexList {
            id1,
            id2,
            id3,
            num,
            desc,
        })
    }

    pub const __ID__: i32 = 1966847134;
}

#[derive(bevy::reflect::Reflect, Debug)]
pub struct NullIndexList {
    pub x: i32,
    pub y: i32,
}

impl NullIndexList {
    pub fn new(mut buf: &mut atom_luban_lib::ByteBuf) -> Result<NullIndexList, LubanError> {
        let x = buf.read_int();
        let y = buf.read_int();

        Ok(NullIndexList { x, y })
    }

    pub const __ID__: i32 = -573800883;
}

#[derive(bevy::reflect::Reflect, Debug)]
pub struct Shape {}

impl Shape {
    pub fn new(
        mut buf: &mut atom_luban_lib::ByteBuf,
    ) -> Result<std::sync::Arc<AbstractBase>, LubanError> {
        let type_id = buf.read_int();
        match type_id {
            crate::test::Circle::__ID__ => Ok(std::sync::Arc::new(crate::test::Circle::new(buf)?)),
            crate::test::Rectangle::__ID__ => {
                Ok(std::sync::Arc::new(crate::test::Rectangle::new(buf)?))
            }
            _ => Err(LubanError::Bean(format!(
                "Invalid type for Shape:{}",
                type_id
            ))),
        }
    }
}

pub trait TShape {}

impl crate::test::TShape for crate::test::Circle {}

impl crate::test::TShape for crate::test::Rectangle {}

impl<'a> GetBase<'a, &'a dyn crate::test::TShape> for AbstractBase {
    fn get_base(&'a self) -> Result<&'a dyn crate::test::TShape, LubanError> {
        let base: Result<&crate::test::Circle, _> = self.try_into();
        if let Ok(r) = base {
            return Ok(r);
        }
        let base: Result<&crate::test::Rectangle, _> = self.try_into();
        if let Ok(r) = base {
            return Ok(r);
        }

        Err(LubanError::Polymorphic(
            "Invalid type for Shape".to_string(),
        ))
    }
}

#[derive(bevy::reflect::Reflect, Debug, atom_macros::TryIntoBase)]
pub struct Circle {
    /// 半径
    pub radius: f32,
}

impl Circle {
    pub fn new(mut buf: &mut atom_luban_lib::ByteBuf) -> Result<Circle, LubanError> {
        let radius = buf.read_float();

        Ok(Circle { radius })
    }

    pub const __ID__: i32 = 2131829196;
}

#[derive(bevy::reflect::Reflect, Debug, atom_macros::TryIntoBase)]
pub struct Rectangle {
    /// 宽度
    pub width: f32,
    /// 高度
    pub height: f32,
}

impl Rectangle {
    pub fn new(mut buf: &mut atom_luban_lib::ByteBuf) -> Result<Rectangle, LubanError> {
        let width = buf.read_float();
        let height = buf.read_float();

        Ok(Rectangle { width, height })
    }

    pub const __ID__: i32 = -31893773;
}

#[derive(bevy::reflect::Reflect, Debug)]
pub struct TestExcelBean1 {
    /// 最高品质
    pub x1: i32,
    /// 黑色的
    pub x2: String,
    /// 蓝色的
    pub x3: i32,
    /// 最差品质
    pub x4: f32,
}

impl TestExcelBean1 {
    pub fn new(mut buf: &mut atom_luban_lib::ByteBuf) -> Result<TestExcelBean1, LubanError> {
        let x1 = buf.read_int();
        let x2 = buf.read_string();
        let x3 = buf.read_int();
        let x4 = buf.read_float();

        Ok(TestExcelBean1 { x1, x2, x3, x4 })
    }

    pub const __ID__: i32 = -1738345160;
}

#[derive(bevy::reflect::Reflect, Debug)]
pub struct TestExcelBean2 {
    /// 最高品质
    pub y1: i32,
    /// 黑色的
    pub y2: String,
    /// 蓝色的
    pub y3: f32,
}

impl TestExcelBean2 {
    pub fn new(mut buf: &mut atom_luban_lib::ByteBuf) -> Result<TestExcelBean2, LubanError> {
        let y1 = buf.read_int();
        let y2 = buf.read_string();
        let y3 = buf.read_float();

        Ok(TestExcelBean2 { y1, y2, y3 })
    }

    pub const __ID__: i32 = -1738345159;
}

#[derive(Debug, bevy::reflect::Reflect, bevy::asset::Asset)]
pub struct TbMultiIndexList {
    pub data_list: Vec<std::sync::Arc<crate::test::MultiIndexList>>,
    pub data_map_id1:
        bevy::platform::collections::HashMap<i32, std::sync::Arc<crate::test::MultiIndexList>>,
    pub data_map_id2:
        bevy::platform::collections::HashMap<i32, std::sync::Arc<crate::test::MultiIndexList>>,
    pub data_map_id3:
        bevy::platform::collections::HashMap<String, std::sync::Arc<crate::test::MultiIndexList>>,
}

impl TbMultiIndexList {
    pub fn new(mut buf: atom_luban_lib::ByteBuf) -> Result<TbMultiIndexList, LubanError> {
        let mut data_list: Vec<std::sync::Arc<crate::test::MultiIndexList>> = vec![];

        for x in (0..buf.read_size()).rev() {
            let row = std::sync::Arc::new(crate::test::MultiIndexList::new(&mut buf)?);
            data_list.push(row.clone());
        }
        let mut data_map_id1: bevy::platform::collections::HashMap<
            i32,
            std::sync::Arc<crate::test::MultiIndexList>,
        > = Default::default();
        let mut data_map_id2: bevy::platform::collections::HashMap<
            i32,
            std::sync::Arc<crate::test::MultiIndexList>,
        > = Default::default();
        let mut data_map_id3: bevy::platform::collections::HashMap<
            String,
            std::sync::Arc<crate::test::MultiIndexList>,
        > = Default::default();
        for x in &data_list {
            data_map_id1.insert(x.id1.clone(), x.clone());
            data_map_id2.insert(x.id2.clone(), x.clone());
            data_map_id3.insert(x.id3.clone(), x.clone());
        }

        Ok(TbMultiIndexList {
            data_list,
            data_map_id1,
            data_map_id2,
            data_map_id3,
        })
    }

    pub fn get_by_id1(&self, key: &i32) -> Option<std::sync::Arc<crate::test::MultiIndexList>> {
        self.data_map_id1.get(key).map(|x| x.clone())
    }
    pub fn get_by_id2(&self, key: &i32) -> Option<std::sync::Arc<crate::test::MultiIndexList>> {
        self.data_map_id2.get(key).map(|x| x.clone())
    }
    pub fn get_by_id3(&self, key: &String) -> Option<std::sync::Arc<crate::test::MultiIndexList>> {
        self.data_map_id3.get(key).map(|x| x.clone())
    }
}

impl atom_luban_lib::table::Table for TbMultiIndexList {
    type Value = std::sync::Arc<crate::test::MultiIndexList>;
}
impl atom_luban_lib::table::ListTable for TbMultiIndexList {}

#[derive(Debug, bevy::reflect::Reflect)]
pub enum TbMultiIndexListKey {
    Id1(i32),
    Id2(i32),
    Id3(String),
}

#[derive(Debug)]
pub enum TbMultiIndexListMap<'a> {
    Id1(&'a bevy::platform::collections::HashMap<i32, std::sync::Arc<crate::test::MultiIndexList>>),
    Id2(&'a bevy::platform::collections::HashMap<i32, std::sync::Arc<crate::test::MultiIndexList>>),
    Id3(
        &'a bevy::platform::collections::HashMap<
            String,
            std::sync::Arc<crate::test::MultiIndexList>,
        >,
    ),
}

impl<'a> atom_luban_lib::table::MultiIndexListTable<'a> for TbMultiIndexList {
    type Key = TbMultiIndexListKey;
    type List = Vec<std::sync::Arc<crate::test::MultiIndexList>>;
    type Map = TbMultiIndexListMap<'a>;

    fn get_row_by(&self, key: &Self::Key) -> Option<Self::Value> {
        match key {
            TbMultiIndexListKey::Id1(key) => self.data_map_id1.get(key).map(|x| x.clone()),
            TbMultiIndexListKey::Id2(key) => self.data_map_id2.get(key).map(|x| x.clone()),
            TbMultiIndexListKey::Id3(key) => self.data_map_id3.get(key).map(|x| x.clone()),
        }
    }

    fn get_data_list(&self) -> &Self::List {
        &self.data_list
    }

    fn get_data_map_by(&'a self, key: &Self::Key) -> Self::Map {
        match key {
            TbMultiIndexListKey::Id1(key) => TbMultiIndexListMap::Id1(&self.data_map_id1),
            TbMultiIndexListKey::Id2(key) => TbMultiIndexListMap::Id2(&self.data_map_id2),
            TbMultiIndexListKey::Id3(key) => TbMultiIndexListMap::Id3(&self.data_map_id3),
        }
    }
}

#[derive(Debug, Default, bevy::reflect::TypePath)]
pub struct TbMultiIndexListLoader;

impl bevy::asset::AssetLoader for TbMultiIndexListLoader {
    type Asset = TbMultiIndexList;

    type Settings = ();

    type Error = TableLoaderError;

    async fn load(
        &self,
        reader: &mut dyn bevy::asset::io::Reader,
        settings: &Self::Settings,
        load_context: &mut bevy::asset::LoadContext<'_>,
    ) -> Result<Self::Asset, Self::Error> {
        bevy::log::info!("TbMultiIndexListLoader loading start");
        let mut bytes = Vec::new();
        use bevy::asset::AsyncReadExt;
        reader.read_to_end(&mut bytes).await?;
        let buf = atom_luban_lib::ByteBuf::new(bytes);
        let tb = TbMultiIndexList::new(buf).unwrap();
        bevy::log::info!("TbMultiIndexListLoader loading over");
        Ok(tb)
    }

    fn extensions(&self) -> &[&str] {
        &["bytes"]
    }
}

#[derive(Debug, bevy::reflect::Reflect, bevy::asset::Asset)]
pub struct TbMultiUnionIndexList {
    pub data_list: Vec<std::sync::Arc<crate::test::MultiUnionIndexList>>,
    pub data_map_union: bevy::platform::collections::HashMap<
        (i32, i32, String),
        std::sync::Arc<crate::test::MultiUnionIndexList>,
    >,
}

impl TbMultiUnionIndexList {
    pub fn new(mut buf: atom_luban_lib::ByteBuf) -> Result<TbMultiUnionIndexList, LubanError> {
        let mut data_list: Vec<std::sync::Arc<crate::test::MultiUnionIndexList>> = vec![];

        for x in (0..buf.read_size()).rev() {
            let row = std::sync::Arc::new(crate::test::MultiUnionIndexList::new(&mut buf)?);
            data_list.push(row.clone());
        }
        let mut data_map_union: bevy::platform::collections::HashMap<
            (i32, i32, String),
            std::sync::Arc<crate::test::MultiUnionIndexList>,
        > = Default::default();
        for x in &data_list {
            data_map_union.insert((x.id1, x.id2, x.id3.clone()), x.clone());
        }

        Ok(TbMultiUnionIndexList {
            data_list,
            data_map_union,
        })
    }

    pub fn get(
        &self,
        key: &(i32, i32, String),
    ) -> Option<std::sync::Arc<crate::test::MultiUnionIndexList>> {
        self.data_map_union.get(key).map(|x| x.clone())
    }
}

impl atom_luban_lib::table::Table for TbMultiUnionIndexList {
    type Value = std::sync::Arc<crate::test::MultiUnionIndexList>;
}
impl atom_luban_lib::table::ListTable for TbMultiUnionIndexList {}

pub type TbMultiUnionIndexListKey = (i32, i32, String);
#[derive(
    Debug,
    Default,
    Clone,
    bevy::reflect::Reflect,
    bevy::prelude::Component,
    serde::Serialize,
    serde::Deserialize,
)]
pub struct TbMultiUnionIndexListRow {
    pub key: TbMultiUnionIndexListKey,
    #[serde(skip)]
    pub data: Option<std::sync::Arc<crate::test::MultiUnionIndexList>>,
}

impl PartialEq for TbMultiUnionIndexListRow {
    fn eq(&self, other: &Self) -> bool {
        self.key == other.key
    }
}

impl atom_luban_lib::table::MultiUnionIndexListTable for TbMultiUnionIndexList {
    type Key = TbMultiUnionIndexListKey;
    type List = Vec<std::sync::Arc<crate::test::MultiUnionIndexList>>;
    type Map = bevy::platform::collections::HashMap<Self::Key, Self::Value>;

    fn get_row_by_key(&self, key: &Self::Key) -> Option<Self::Value> {
        self.data_map_union.get(key).map(|x| x.clone())
    }

    fn get_data_list(&self) -> &Self::List {
        &self.data_list
    }

    fn get_data_map(&self) -> &Self::Map {
        &self.data_map_union
    }
}

#[derive(Debug, Default, bevy::reflect::TypePath)]
pub struct TbMultiUnionIndexListLoader;

impl bevy::asset::AssetLoader for TbMultiUnionIndexListLoader {
    type Asset = TbMultiUnionIndexList;

    type Settings = ();

    type Error = TableLoaderError;

    async fn load(
        &self,
        reader: &mut dyn bevy::asset::io::Reader,
        settings: &Self::Settings,
        load_context: &mut bevy::asset::LoadContext<'_>,
    ) -> Result<Self::Asset, Self::Error> {
        bevy::log::info!("TbMultiUnionIndexListLoader loading start");
        let mut bytes = Vec::new();
        use bevy::asset::AsyncReadExt;
        reader.read_to_end(&mut bytes).await?;
        let buf = atom_luban_lib::ByteBuf::new(bytes);
        let tb = TbMultiUnionIndexList::new(buf).unwrap();
        bevy::log::info!("TbMultiUnionIndexListLoader loading over");
        Ok(tb)
    }

    fn extensions(&self) -> &[&str] {
        &["bytes"]
    }
}

#[derive(Debug, bevy::reflect::Reflect, bevy::asset::Asset)]
pub struct TbNullIndexList {
    pub data_list: Vec<std::sync::Arc<crate::test::NullIndexList>>,
}

impl TbNullIndexList {
    pub fn new(mut buf: atom_luban_lib::ByteBuf) -> Result<TbNullIndexList, LubanError> {
        let mut data_list: Vec<std::sync::Arc<crate::test::NullIndexList>> = vec![];

        for x in (0..buf.read_size()).rev() {
            let row = std::sync::Arc::new(crate::test::NullIndexList::new(&mut buf)?);
            data_list.push(row.clone());
        }

        Ok(TbNullIndexList { data_list })
    }
}

impl atom_luban_lib::table::Table for TbNullIndexList {
    type Value = std::sync::Arc<crate::test::NullIndexList>;
}
impl atom_luban_lib::table::ListTable for TbNullIndexList {}

impl atom_luban_lib::table::NotIndexListTable for TbNullIndexList {
    type List = Vec<Self::Value>;
    fn iter(&self) -> impl Iterator<Item = &Self::Value> {
        self.data_list.iter()
    }

    fn get_data_list(&self) -> &Self::List {
        &self.data_list
    }
}

#[derive(Debug, Default, bevy::reflect::TypePath)]
pub struct TbNullIndexListLoader;

impl bevy::asset::AssetLoader for TbNullIndexListLoader {
    type Asset = TbNullIndexList;

    type Settings = ();

    type Error = TableLoaderError;

    async fn load(
        &self,
        reader: &mut dyn bevy::asset::io::Reader,
        settings: &Self::Settings,
        load_context: &mut bevy::asset::LoadContext<'_>,
    ) -> Result<Self::Asset, Self::Error> {
        bevy::log::info!("TbNullIndexListLoader loading start");
        let mut bytes = Vec::new();
        use bevy::asset::AsyncReadExt;
        reader.read_to_end(&mut bytes).await?;
        let buf = atom_luban_lib::ByteBuf::new(bytes);
        let tb = TbNullIndexList::new(buf).unwrap();
        bevy::log::info!("TbNullIndexListLoader loading over");
        Ok(tb)
    }

    fn extensions(&self) -> &[&str] {
        &["bytes"]
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use atom_luban_lib::table::{MultiIndexListTable, MultiUnionIndexListTable, NotIndexListTable};
    use bevy::asset::AssetLoader;

    fn multi_index_bytes(id1: i32, id2: i32, id3: &str, num: i32, desc: &str) -> Vec<u8> {
        let mut b = crate::testutil::enc_int(id1);
        b.extend_from_slice(&crate::testutil::enc_int(id2));
        b.extend_from_slice(&crate::testutil::enc_string(id3));
        b.extend_from_slice(&crate::testutil::enc_int(num));
        b.extend_from_slice(&crate::testutil::enc_string(desc));
        b
    }

    fn null_index_bytes(x: i32, y: i32) -> Vec<u8> {
        let mut b = crate::testutil::enc_int(x);
        b.extend_from_slice(&crate::testutil::enc_int(y));
        b
    }

    #[test]
    fn access_flag_constants_and_ops() {
        assert_eq!(AccessFlag::WRITE.bits(), 1);
        assert_eq!(AccessFlag::READ.bits(), 2);
        assert_eq!(AccessFlag::TRUNCATE.bits(), 4);
        assert_eq!(AccessFlag::NEW.bits(), 8);
        assert_eq!(AccessFlag::READ_WRITE.bits(), 3);

        assert!(AccessFlag::READ_WRITE.contains(AccessFlag::READ));
        assert!(AccessFlag::READ_WRITE.contains(AccessFlag::WRITE));
        assert!(!AccessFlag::READ_WRITE.contains(AccessFlag::NEW));

        let combined = AccessFlag::READ | AccessFlag::NEW;
        assert_eq!(combined.bits(), 10);
        assert!(combined.intersects(AccessFlag::NEW));
        assert!(!combined.intersects(AccessFlag::TRUNCATE));

        let mut flags = AccessFlag::empty();
        flags.insert(AccessFlag::WRITE);
        assert!(flags.contains(AccessFlag::WRITE));
        flags.remove(AccessFlag::WRITE);
        assert!(!flags.contains(AccessFlag::WRITE));

        assert_eq!(AccessFlag::from_bits_truncate(3), AccessFlag::READ_WRITE);
        assert_eq!(
            AccessFlag::READ_WRITE | AccessFlag::NEW,
            AccessFlag::from_bits_truncate(11)
        );
    }

    #[test]
    fn multi_index_list_new_parses() {
        let mut buf = atom_luban_lib::ByteBuf::new(multi_index_bytes(1, 10, "k", 5, "first"));
        let m = MultiIndexList::new(&mut buf).expect("MultiIndexList 解析失败");
        assert_eq!((m.id1, m.id2), (1, 10));
        assert_eq!(m.id3, "k");
        assert_eq!((m.num, m.desc.as_str()), (5, "first"));
        assert_eq!(MultiIndexList::__ID__, 2016237651);
    }

    #[test]
    fn multi_union_index_list_new_parses() {
        let mut buf = atom_luban_lib::ByteBuf::new(multi_index_bytes(2, 20, "u", 6, "union"));
        let m = MultiUnionIndexList::new(&mut buf).expect("MultiUnionIndexList 解析失败");
        assert_eq!((m.id1, m.id2, m.id3.as_str()), (2, 20, "u"));
        assert_eq!((m.num, m.desc.as_str()), (6, "union"));
        assert_eq!(MultiUnionIndexList::__ID__, 1966847134);
    }

    #[test]
    fn null_index_list_new_parses() {
        let mut buf = atom_luban_lib::ByteBuf::new(null_index_bytes(3, 4));
        let n = NullIndexList::new(&mut buf).expect("NullIndexList 解析失败");
        assert_eq!((n.x, n.y), (3, 4));
        assert_eq!(NullIndexList::__ID__, -573800883);
    }

    #[test]
    fn test_excel_bean_new_parses() {
        let mut b1 = crate::testutil::enc_int(1);
        b1.extend_from_slice(&crate::testutil::enc_string("black"));
        b1.extend_from_slice(&crate::testutil::enc_int(2));
        b1.extend_from_slice(&crate::testutil::enc_float(0.5));
        let mut buf = atom_luban_lib::ByteBuf::new(b1);
        let bean1 = TestExcelBean1::new(&mut buf).expect("TestExcelBean1 解析失败");
        assert_eq!((bean1.x1, bean1.x2.as_str(), bean1.x3), (1, "black", 2));
        assert_eq!(bean1.x4, 0.5);
        assert_eq!(TestExcelBean1::__ID__, -1738345160);

        // "orange"(6) 使 y3 落在 8 对齐偏移上
        let mut b2 = crate::testutil::enc_int(7);
        b2.extend_from_slice(&crate::testutil::enc_string("orange"));
        b2.extend_from_slice(&crate::testutil::enc_float(1.25));
        let mut buf = atom_luban_lib::ByteBuf::new(b2);
        let bean2 = TestExcelBean2::new(&mut buf).expect("TestExcelBean2 解析失败");
        assert_eq!((bean2.y1, bean2.y2.as_str()), (7, "orange"));
        assert_eq!(bean2.y3, 1.25);
        assert_eq!(TestExcelBean2::__ID__, -1738345159);
    }

    #[test]
    fn shape_new_circle() {
        // 多态分发：type_id 先行（5 字节），radius 偏移非 8 对齐 → 值恒读 0.0，仅验证类型
        let mut bytes = crate::testutil::enc_int(Circle::__ID__);
        bytes.extend_from_slice(&crate::testutil::enc_float(2.5));
        let mut buf = atom_luban_lib::ByteBuf::new(bytes);
        let shape = Shape::new(&mut buf).expect("Shape::new(Circle) 解析失败");
        let circle = shape.downcast_ref::<Circle>().expect("下转发 Circle");
        assert_eq!(circle.radius, 0.0);
        assert_eq!(Circle::__ID__, 2131829196);
    }

    #[test]
    fn circle_new_parses() {
        let mut buf = atom_luban_lib::ByteBuf::new(crate::testutil::enc_float(2.5));
        let c = Circle::new(&mut buf).expect("Circle 解析失败");
        assert_eq!(c.radius, 2.5);
    }

    #[test]
    fn shape_new_rectangle() {
        let mut bytes = crate::testutil::enc_int(Rectangle::__ID__);
        bytes.extend_from_slice(&crate::testutil::enc_float(3.0));
        bytes.extend_from_slice(&crate::testutil::enc_float(4.0));
        let mut buf = atom_luban_lib::ByteBuf::new(bytes);
        let shape = Shape::new(&mut buf).expect("Shape::new(Rectangle) 解析失败");
        let rect = shape.downcast_ref::<Rectangle>().expect("下转发 Rectangle");
        assert_eq!((rect.width, rect.height), (0.0, 0.0));
        assert_eq!(Rectangle::__ID__, -31893773);
    }

    #[test]
    fn rectangle_new_parses() {
        // width@0 对齐读真实值，height@4 恒读 0.0
        let mut bytes = crate::testutil::enc_float(3.0);
        bytes.extend_from_slice(&crate::testutil::enc_float(0.0));
        let mut buf = atom_luban_lib::ByteBuf::new(bytes);
        let r = Rectangle::new(&mut buf).expect("Rectangle 解析失败");
        assert_eq!((r.width, r.height), (3.0, 0.0));
    }

    #[test]
    fn shape_new_invalid_type() {
        let mut buf = atom_luban_lib::ByteBuf::new(crate::testutil::enc_int(12345));
        let err = Shape::new(&mut buf).expect_err("未知类型应报错");
        assert!(matches!(err, LubanError::Bean(_)));
    }

    #[test]
    fn get_base_downcasts() {
        let circle: std::sync::Arc<AbstractBase> = std::sync::Arc::new(Circle { radius: 1.0 });
        let base = circle.get_base().expect("Circle get_base");
        assert_eq!(std::mem::size_of_val(base), std::mem::size_of::<Circle>());

        let rect: std::sync::Arc<AbstractBase> = std::sync::Arc::new(Rectangle {
            width: 2.0,
            height: 3.0,
        });
        let base = rect.get_base().expect("Rectangle get_base");
        assert_eq!(
            std::mem::size_of_val(base),
            std::mem::size_of::<Rectangle>()
        );
    }

    #[test]
    fn get_base_invalid_type() {
        let obj: std::sync::Arc<AbstractBase> =
            std::sync::Arc::new(crate::vector2 { x: 1.0, y: 2.0 });
        let err = match obj.get_base() {
            Ok(_) => panic!("vector2 不应被识别为 Shape"),
            Err(e) => e,
        };
        assert!(matches!(err, LubanError::Polymorphic(_)));
    }

    #[test]
    fn tb_multi_index_list_new_and_lookup() {
        let bytes = crate::testutil::table_bytes(vec![
            multi_index_bytes(1, 10, "x", 5, "first"),
            multi_index_bytes(2, 20, "y", 6, "second"),
        ]);
        let tb = TbMultiIndexList::new(atom_luban_lib::ByteBuf::new(bytes))
            .expect("TbMultiIndexList 解析失败");
        assert_eq!(tb.data_list.len(), 2);

        let m = tb.get_by_id1(&1).expect("按 id1=1 查询");
        assert_eq!(m.num, 5);
        assert!(tb.get_by_id1(&99).is_none());

        let m = tb.get_by_id2(&20).expect("按 id2=20 查询");
        assert_eq!(m.num, 6);
        assert!(tb.get_by_id2(&99).is_none());

        let m = tb.get_by_id3(&"y".to_string()).expect("按 id3=y 查询");
        assert_eq!(m.desc, "second");
        assert!(tb.get_by_id3(&"z".to_string()).is_none());

        let m = tb
            .get_row_by(&TbMultiIndexListKey::Id1(1))
            .expect("key Id1");
        assert_eq!(m.id2, 10);
        let m = tb
            .get_row_by(&TbMultiIndexListKey::Id2(20))
            .expect("key Id2");
        assert_eq!(m.id1, 2);
        let m = tb
            .get_row_by(&TbMultiIndexListKey::Id3("x".to_string()))
            .expect("key Id3");
        assert_eq!(m.num, 5);
        assert!(tb.get_row_by(&TbMultiIndexListKey::Id1(999)).is_none());

        assert_eq!(tb.get_data_list().len(), 2);

        let map = tb.get_data_map_by(&TbMultiIndexListKey::Id1(1));
        assert!(matches!(map, TbMultiIndexListMap::Id1(m) if m.len() == 2));
        let map = tb.get_data_map_by(&TbMultiIndexListKey::Id2(2));
        assert!(matches!(map, TbMultiIndexListMap::Id2(m) if m.contains_key(&20)));
        let map = tb.get_data_map_by(&TbMultiIndexListKey::Id3("z".to_string()));
        assert!(matches!(map, TbMultiIndexListMap::Id3(m) if m.contains_key("x")));
    }

    #[test]
    fn tb_multi_union_index_list_new_and_lookup() {
        let bytes = crate::testutil::table_bytes(vec![
            multi_index_bytes(1, 2, "a", 5, "first"),
            multi_index_bytes(3, 4, "b", 6, "second"),
        ]);
        let tb = TbMultiUnionIndexList::new(atom_luban_lib::ByteBuf::new(bytes))
            .expect("TbMultiUnionIndexList 解析失败");
        assert_eq!(tb.data_list.len(), 2);
        assert_eq!(tb.data_map_union.len(), 2);

        let m = tb.get(&(1, 2, "a".to_string())).expect("联合键 (1,2,a)");
        assert_eq!(m.num, 5);
        assert!(tb.get(&(1, 2, "b".to_string())).is_none());

        let m = tb
            .get_row_by_key(&(3, 4, "b".to_string()))
            .expect("MultiUnionIndexListTable");
        assert_eq!(m.num, 6);
        assert!(tb.get_row_by_key(&(0, 0, "z".to_string())).is_none());
        assert_eq!(tb.get_data_list().len(), 2);
        assert_eq!(tb.get_data_map().len(), 2);
    }

    #[test]
    fn tb_null_index_list_new_and_iter() {
        let bytes =
            crate::testutil::table_bytes(vec![null_index_bytes(1, 2), null_index_bytes(3, 4)]);
        let tb = TbNullIndexList::new(atom_luban_lib::ByteBuf::new(bytes))
            .expect("TbNullIndexList 解析失败");
        assert_eq!(tb.get_data_list().len(), 2);
        let sum_x: i32 = tb.iter().map(|r| r.x).sum();
        let sum_y: i32 = tb.iter().map(|r| r.y).sum();
        assert_eq!((sum_x, sum_y), (4, 6));
    }

    #[test]
    fn tb_multi_union_index_list_row_partial_eq() {
        let a = TbMultiUnionIndexListRow {
            key: (1, 2, "a".to_string()),
            data: None,
        };
        let b = TbMultiUnionIndexListRow {
            key: (1, 2, "a".to_string()),
            data: None,
        };
        assert_eq!(a, b);
        let c = TbMultiUnionIndexListRow {
            key: (1, 2, "b".to_string()),
            data: None,
        };
        assert_ne!(a, c);
    }

    #[test]
    fn loaders_extensions() {
        assert_eq!(TbMultiIndexListLoader.extensions(), &["bytes"]);
        assert_eq!(TbMultiUnionIndexListLoader.extensions(), &["bytes"]);
        assert_eq!(TbNullIndexListLoader.extensions(), &["bytes"]);
    }

    fn assert_reflects<T: bevy::reflect::PartialReflect>(value: &T) {
        use bevy::reflect::DynamicTypePath;
        assert!(value.get_represented_type_info().is_some());
        let _ = value.reflect_ref();
        let _ = value.reflect_type_path();
    }

    #[test]
    fn reflect_api_smoke() {
        let mut buf = atom_luban_lib::ByteBuf::new(multi_index_bytes(1, 2, "k", 3, "d"));
        let m = MultiIndexList::new(&mut buf).expect("MultiIndexList 解析失败");
        assert_reflects(&m);
        let mut buf = atom_luban_lib::ByteBuf::new(multi_index_bytes(1, 2, "k", 3, "d"));
        let m = MultiUnionIndexList::new(&mut buf).expect("MultiUnionIndexList 解析失败");
        assert_reflects(&m);
        let mut buf = atom_luban_lib::ByteBuf::new(null_index_bytes(1, 2));
        let n = NullIndexList::new(&mut buf).expect("NullIndexList 解析失败");
        assert_reflects(&n);

        let mut buf = atom_luban_lib::ByteBuf::new(crate::testutil::enc_float(1.0));
        let c = Circle::new(&mut buf).expect("Circle 解析失败");
        assert_reflects(&c);
        let mut rect_bytes = crate::testutil::enc_float(1.0);
        rect_bytes.extend_from_slice(&crate::testutil::enc_float(0.0));
        let r = Rectangle::new(&mut atom_luban_lib::ByteBuf::new(rect_bytes))
            .expect("Rectangle 解析失败");
        assert_reflects(&r);

        let mut b1 = crate::testutil::enc_int(1);
        b1.extend_from_slice(&crate::testutil::enc_string("black"));
        b1.extend_from_slice(&crate::testutil::enc_int(2));
        b1.extend_from_slice(&crate::testutil::enc_float(0.5));
        let bean1 = TestExcelBean1::new(&mut atom_luban_lib::ByteBuf::new(b1))
            .expect("TestExcelBean1 解析失败");
        assert_reflects(&bean1);
        let mut b2 = crate::testutil::enc_int(7);
        b2.extend_from_slice(&crate::testutil::enc_string("orange"));
        b2.extend_from_slice(&crate::testutil::enc_float(1.25));
        let bean2 = TestExcelBean2::new(&mut atom_luban_lib::ByteBuf::new(b2))
            .expect("TestExcelBean2 解析失败");
        assert_reflects(&bean2);

        let rows = crate::testutil::table_bytes(vec![
            multi_index_bytes(1, 10, "x", 5, "first"),
            multi_index_bytes(2, 20, "y", 6, "second"),
        ]);
        let tb = TbMultiIndexList::new(atom_luban_lib::ByteBuf::new(rows))
            .expect("TbMultiIndexList 解析失败");
        assert_reflects(&tb);
        assert_reflects(&TbMultiIndexListKey::Id1(1));

        let rows = crate::testutil::table_bytes(vec![multi_index_bytes(1, 2, "a", 5, "first")]);
        let tb = TbMultiUnionIndexList::new(atom_luban_lib::ByteBuf::new(rows))
            .expect("TbMultiUnionIndexList 解析失败");
        assert_reflects(&tb);
        assert_reflects(&TbMultiUnionIndexListRow {
            key: (1, 2, "a".to_string()),
            data: None,
        });

        let rows = crate::testutil::table_bytes(vec![null_index_bytes(1, 2)]);
        let tb = TbNullIndexList::new(atom_luban_lib::ByteBuf::new(rows))
            .expect("TbNullIndexList 解析失败");
        assert_reflects(&tb);
    }
}
