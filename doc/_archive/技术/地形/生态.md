
首先创建chunk mesh的时候，返回voxel的biome类型。

1. 这一步需要有地形的biome类型，
	1. 16km的地形使用1k的生态类型，则一个pixel代表一个Chunk。
	2. 
2. 而且默认生成的生态类型只有地表的，缺少地下的。
	1. 暂不考虑，太复杂。

之后根据chunk所拥有的biome类型，来使用对应的材质。

1. 材质过渡要怎么搞？
	1. 如何使用splat map。但是由于随即地形，splat map的数量过于惊人，没办法使用。。。。。
	2. 直接根据顶点的生态权重，做权重混合，但是效果不好。
	3. 表现不是什么重点，先做混合权重。
2. 根据voxel的八个角的biome的比例，使用8个bind group(每个bind group对应一种biome，里面有这种biome的所有材质数据),（bind group 太大需要合并，避免兼容问题）