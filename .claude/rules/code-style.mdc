---
alwaysApply: true
---

# 代码风格

## 错误处理

- 禁止 `unwrap()`，统一使用 `expect("原因")`
- `panic!` 仅在硬停止场景使用（如找不到项目根目录标记文件）
- 可恢复场景用 `warn!` 记录并跳过
- 不使用 `thiserror`/`anyhow`

## 命名

- `snake_case` 模块/函数，`CamelCase` 类型，`UPPER_CASE` 常量
- mod.rs 模式组织模块

## 注释

- 非平凡逻辑用中文注释
- 简单辅助函数用英文
- Shader 中英文混合

## 格式化

- 使用 `rustfmt.toml` 配置（Unix 换行、field init shorthand、edition 2024）
