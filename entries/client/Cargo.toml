[package]
name = "client"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
terrain = { path = "../../crates/terrain" }
bevy = { version = "0.11.2", features = ["trace", "trace_tracy"] }

bevy_mod_debugdump = {  version = "0.8.1", optional = true }

[features]
default = []
debug_dump = ["dep:bevy_mod_debugdump"]

[dev-dependencies]